# خطة الاختبار الشاملة — متجر نداف

> النظام مبنيّ بـ**VIBE CODING**: كود يعمل ويبدو صحيحًا، لكن **لا أحد يعرف
> حدوده**. وهذه هي المشكلة الحقيقية: الشيفرة لا تكذب، لكنها **لا تخبرك بما لم
> تُجرّبه**. فالفجوة ليست في الكود بل في **ما لم يُختبر بعد**.

---

## المبدأ: ما الذي يميّز اختبار نظام مبني بالسريع؟

النظام المبني بالسريع يمرّ في **المسار السعيد** (happy path) الذي جرّبه بانيه،
ويسقط في كل ما سواه. فالاختبارات المطلوبة هي التي تسأل:

| السؤال | الفئة |
|---|---|
| ماذا لو فعل المستخدم شيئًا **لم يتوقعه** الباني؟ | حالات حدّية |
| ماذا لو **تزامن** مستخدمان؟ | تزامن |
| ماذا لو حاول **شخص آخر** الوصول؟ | أمن |
| ماذا لو **تغيّرت البيئة** (نفق · استضافة · وكيل)؟ | نشر |
| ماذا لو **تغيّرت البيانات** بعد الطلب؟ | سلامة تاريخية |
| هل يعمل **كل** مسار، لا الذي جرّبته فقط؟ | مرور شامل |

---

## ✅ المُغطّى الآن (٣٢٦ اختبارًا)

| المجال | أمثلة |
|---|---|
| لوحة الإدارة | كل صفحة تُرسم · التبويبات · الأعمدة · الأزرار |
| الصلاحيات | كل دور × كل مورد · حماية المالك |
| الدفع | بوابة إثبات الدفع · تهيئة الوسائل · الكوبونات |
| الأسعار | الجملة · المخفي · العملتان |
| الطلب | الخدمة · الضيف · النافذة الإلزامية · واتساب |
| المخزون | الخصم · الحركات · التقارير |
| الفواتير | النصّ الكامل · الأختام · تطبيع الأرقام |
| المتجر | مرور على كل مسار · الكروت · الفلاتر |
| الإعدادات | سلامة الحفظ (لا يُمحى حقل) |
| النشر | الوكيل العكسي · HTTPS |

---

## ⚠️ الفجوات — مرتّبة بالخطورة

### 🔴 ١. الأمن — أعلاها خطورة
- [ ] **تزوير الطلبات (IDOR)**: هل يستطيع عميل فتح فاتورة عميل آخر بتغيير الرمز؟
- [ ] **رفع الملفات**: مسار مخرِج (`../`)، نوع مزيّف (`.php.png`)، حجم أكبر من الحد
- [ ] **حقن HTML/XSS**: اسم عميل أو ملاحظة تحتوي `<script>` — هل يُهرَّب في الفاتورة واللوحة وتيليجرام؟
- [ ] **تجاوز الصلاحيات عبر الطلب المباشر**: هل يستطيع `warehouse` تنفيذ فعل `manager` بنداء Livewire مباشر (لا بالزر المخفي)؟
- [ ] **الحماية من التخمين**: `/account/orders/{code}` — هل رمز الطلب قابل للتعداد؟
- [ ] **CSRF**: هل كل مسار كتابة محميّ؟

### 🔴 ٢. التزامن — عطل صامت يكلّف مالًا
- [ ] طلبان متزامنان على **آخر قطعة**: هل يبيع أحدهما ما لا يملك؟
- [ ] كوبون بحدّ استخدام واحد: هل يمرّ طلبان معًا؟
- [ ] تعديل مخزون من اللوحة أثناء شراء جارٍ
- [ ] **تحديث متزامن لنفس الطلب** من أدمنين

### 🟠 ٣. سلامة البيانات — العلاقات الحسابية
- [ ] `total = subtotal − discount + shipping` في **كل** حالة (بكوبون · بدون · شحن مجاني)
- [ ] مجموع بنود الطلب = مجموع الطلب
- [ ] المخزون لا يصير سالبًا في أي مسار
- [ ] حذف منتج له طلبات — هل تبقى الفواتير سليمة؟
- [ ] تعديل سعر منتج بعد الطلب — هل يتغيّر الطلب القديم؟ (**يجب ألّا يتغيّر**)

### 🟠 ٤. البيئة والنشر — وهو ما كسر العرض فعلًا
- [ ] خلف وكيل: الأصول `https` (مُغطّى الآن)
- [ ] `APP_ENV=production`: صفحة الخطأ لا تكشف مسارات ولا استعلامات
- [ ] `APP_DEBUG=false`
- [ ] تظهر الصور على الاستضافة (المتجر يكتب في `public/storage` بلا رابط رمزي — تأكد أن المجلد رُفع كاملًا مع حزمة النشر)
- [ ] `php artisan config:cache` — هل يعمل التطبيق بعده؟
- [ ] **قاعدة البيانات MySQL** لا SQLite فقط (الإنتاج عليها)

### 🟡 ٥. حالات حدّية في الواجهة
- [ ] البحث بلا نتائج · بكلمة واحدة · بحرف · برموز (`%` · `_` · `<`)
- [ ] سلة فارغة → الدفع مباشرة
- [ ] منتج بلا صور · بلا متغيّرات · بلا سعر
- [ ] نصّ عربي طويل جدًا (اسم منتج ٢٠٠ حرف)
- [ ] رقم هاتف غريب (حروف · رموز · فارغ)
- [ ] **الجوال**: لا يمكن اختباره بـPHPUnit — يلزم فحص بصري بعرض ٣٧٥ بكسل

### 🟡 ٦. الإشعارات والبريد
- [ ] إشعار تيليجرام عند كل تغيّر حالة (مُغطّى جزئيًّا)
- [ ] بريد استعادة كلمة المرور — يُرسل فعلًا وبالمحتوى الصحيح
- [ ] فشل الإرسال لا يُسقط الطلب

---

## الطريقة المقترحة

### المرحلة ١ — الأمن والتزامن (الأعلى مردودًا)
عطل أمني واحد يكلّف أكثر من كل ما سواه. ابدأ بـIDOR وXSS وتجاوز الصلاحيات.

### المرحلة ٢ — سلامة البيانات
اختبارات حسابية بسيطة تكشف أخطاء جمع وخصم لا يلاحظها أحد حتى يراجع المالك دفاتره.

### المرحلة ٣ — البيئة
اختبار واحد يمنع تكرار ما حدث مع العميل: **افحص دائمًا على عنوان عام قبل العرض**.

### المرحلة ٤ — الحدود والواجهة
أقلّها خطورة وأكثرها عددًا — تُؤجَّل بعد استقرار ما قبلها.

---

## قواعد الاختبار في هذا المشروع

1. **اختبر الخدمة لا الواجهة** حين يتعلق الأمر بالمنع — الواجهة تجميل، والخدمة حارس.
   (بوابة إثبات الدفع تُقاس على `CheckoutService` مباشرة، لأنها الطريق المفتوح.)
2. **كل عطل يُصلَح يُكتَب له اختبار** — وإلا عاد بصمت.
3. **بيئة الاختبار معزولة**: لا تعتمد على `.env` المطوّر (كل عنوان يُثبَّت في `phpunit.xml`).
4. **الاختبار يقيس القاعدة لا الحالة**: سؤال بلا جواب **يظهر**، وبجواب **يختفي** —
   فيبقى صحيحًا لو تغيّر النموذج.
5. **عند تغيير البيانات لا يُقاس المحتوى**: قارن بالعلاقة (مجموع = إجمالي) لا برقم ثابت.

---

## تشغيل الاختبارات

```bash
cd A:\nadaf
E:\tools\php83\php.exe vendor/bin/phpunit                    # الكل
E:\tools\php83\php.exe vendor/bin/phpunit --filter Cart      # ملف بعينه
E:\tools\php83\php.exe vendor/bin/phpunit --testdox          # بأسماء مقروءة
```

---

## ✅ ما أُضيف فعلًا (جولة «اختبارات قوية»)

أربع حزم جديدة — **٤٤ اختبارًا** — تقيس ما لم يكن يُقاس:

| الملف | الفئة | يقيس |
|---|---|---|
| `SecurityTest` (١٤) | أمني | IDOR على الطلبات والفواتير · تصعيد الصلاحيات عبر النموذج وحقل مُلفَّق · حجب الطاقم فورًا · حقن السكربت في الفاتورة والمنتج وتيليجرام · كشف كلمة المرور · عشوائية رمز الطلب · **CSRF على كل مسار كتابة** |
| `DataIntegrityTest` (١٣) | السلامة | الإجمالي = المجموع − الخصم + الشحن · مجموع البنود = المجموع الفرعي · الليرة تتبع سعر الصرف المثبَّت · المخزون لا يصير سالبًا · **طلب مرفوض لا يترك أثرًا** · إعادة تسمية منتج لا تُغيّر طلبًا قديمًا · حذف منتج لا يُفسد فاتورة · عدّاد الكوبون · تفرد الأكواد |
| `CustomerFlowTest` (٦) | التدفق | رحلة الضيف كاملة (١٠ خطوات) · رحلة المسجَّل · واتساب بلا حساب **ولا طلب ولا خصم مخزون** · طلب بمنتجين · الطلب يصل اللوحة · نفاد المخزون أثناء الرحلة |
| `AiCodeSmellTest` (١١) | أخطاء AI | آثار تصحيح متروكة · علامات TODO · مسارات بلا اسم · **قوالب مفقودة** · **مفاتيح ترجمة مفقودة** · **كل مورد Filament له سياسة** · السياسات ترث `StaffPolicy` · تهجيرات لها `down()` · أسرار في الكود · **N+1 في الصفحات الرئيسية** · صور بلا `alt` |

### وأهم ثلاثة منها

**١. قاعدة المشروع مكتوبة كاختبار.** المشروع يوثّق أن «أي مورد Filament يجب أن
يحصل على صلاحية + سياسة»، و`Filament\authorize()` يعيد `Response::allow()`
عند **غياب** السياسة — أي أن نسيانها يفتح المورد للجميع بلا رسالة.
`test_every_filament_resource_has_a_policy` يمنع النسيان.

**٢. CSRF يُفحَص على المسارات لا على الكود.** الاختبار يمرّ على جدول المسارات
ويفشل إن وُجد مسار كتابة خارج مجموعة `web` — فيمنع إضافة مسار POST مكشوف
في أي كود مستقبلي.

**٣. ثبات المستندات التاريخية.** إعادة تسمية منتج أو حذفه **لا يجوز** أن
تُغيّر طلبًا قديمًا. هذا ما يُكتشف عادة عند مراجعة الدفاتر بعد أشهر.

---

## ⚠️ درس: اختباراتي نفسها كان فيها أخطاء

ثلاثة أخطاء في اختباراتي كشفها تشغيلها — وهي **نفس فئة** أخطاء الكود المولَّد:

| الخطأ | ما جرى |
|---|---|
| `str_contains($code, 'ray(')` | تطابق **`array(`** ⇒ ٢٢ إنذارًا كاذبًا |
| `str_contains($code, 'dd(')` | تطابق **`add(`** ⇒ ٣ إنذارات كاذبة |
| `/<img[^>]*>/` | سهم **`->`** داخل `{{ $x->y }}` فيه `>` فقطع الوسم ⇒ ٧ إنذارات كاذبة |

**الدرس**: البحث النصّي المجرّد يحتاج **حدود كلمة**، والتعبير النمطي يحتاج
احترام بنية اللغة لا شكلها. والإنذار الكاذب أسوأ من غياب الفحص: يُفقد الثقة
بالاختبار كله.

**والدرس الثاني**: اختبار لا يجد شيئًا قد يكون **معطوبًا** لا ناجحًا. فأُضيف
تحقق ذاتي (`assertGreaterThan(0, $checked)`) يمنع أن يبدو اختبار الصور ناجحًا
وهو لا يفحص أي صورة.
