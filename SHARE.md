# مشاركة الموقع مع عميل في دولة أخرى 🌍

ثلاث طرق. اختر بحسب الهدف: **تجربة سريعة اليوم** أم **رابط دائم**.

---

## الطريقة ١ — نفق مؤقت (الأنسب للتجربة) ✅

الموقع يبقى على جهازك، ويُفتح للعالم عبر رابط `https` مؤقت.
**لا حاجة إلى استضافة ولا رفع ملفات.**

### التشغيل

نافذتان طرفيتان — **كلتاهما يجب أن تبقيا مفتوحتين**:

```bash
# النافذة ١ — سيرفر الموقع
cd A:\nadaf
E:\tools\php83\php.exe artisan serve --host=0.0.0.0 --port=8000

# النافذة ٢ — النفق
cd E:\tools\cloudflared
cloudflared.exe tunnel --url http://localhost:8000 --no-autoupdate --protocol http2
```

سيطبع النفق سطرًا كهذا:

```
|  https://addressing-isolated-dedicated-altered.trycloudflare.com  |
```

**هذا هو الرابط الذي ترسله للعميل.**

### ⚠️ أربع نقاط مهمة

**١. العنوان يتغيّر كل تشغيل.** النفق المجاني بلا حساب يمنح عنوانًا عشوائيًا
جديدًا في كل مرة. فأرسل الرابط بعد التأكد من عمله، لا قبله.

**٢. حدّث `APP_URL` بعد كل تشغيل** — وإلا حملت روابط المنتجات في رسائل
واتساب عنوانًا لا يفتحه العميل:

```env
APP_URL=https://<العنوان-الجديد>.trycloudflare.com
```
ثم `php artisan config:clear`.

**٣. لوحة الإدارة محميّة تلقائيًّا.** الوسيط `BlockRemoteAdmin` يمنع `/admin`
من أي مضيف غير محلي حين `APP_ENV=local` — فيردّ **404** من الإنترنت و**200**
من جهازك وشبكتك. فلا تُكشف اللوحة على النفق العام.
*(ولا أثر له على الإنتاج: هناك اللوحة على نطاقها وتُحمى بكلمة مرورها.)*

**٤. جهازك يجب أن يبقى مفتوحًا.** إغلاق النافذتين أو نوم الجهاز يقطع الرابط.

---

## الطريقة ٢ — استضافة حقيقية (رابط دائم)

الأنسب إن أراد العميل متابعة الموقع أيامًا. الخطوات كاملة في **`DEPLOY.md`**
(استضافة InfinityFree المجانية).

⚠️ حدود InfinityFree للرفع **~10 ميغابايت** — لا تكفي فيديوهات السلايدر
الكبيرة. راجع قسم حدود الرفع في `DEPLOY.md`.

---

## الطريقة ٣ — بدائل النفق

| الأداة | الأمر | ملاحظة |
|---|---|---|
| Cloudflare | `cloudflared tunnel --url http://localhost:8000` | **المُجرَّب هنا** · مجاني بلا حساب |
| localhost.run | `ssh -R 80:localhost:8000 nokey@localhost.run` | بلا تثبيت — SSH فقط |
| serveo | `ssh -R 80:localhost:8000 serveo.net` | بلا تثبيت |
| ngrok | `ngrok http 8000` | يحتاج حسابًا وتوكنًا |

**إن فشل Cloudflare** بخطأ `context deadline exceeded` فالشبكة تحجب QUIC —
أضف `--protocol http2` كما في الأمر أعلاه (وهو ما نجح فعلًا هنا).

---

## قبل أن ترسل الرابط — قائمة تحقق

- [ ] النفق يعمل والرابط يفتح من هاتف على شبكة **أخرى** (بيانات الجوال مثلًا)
- [ ] `APP_URL` محدَّث بعنوان النفق
- [ ] `/admin/login` يردّ **404** على الرابط العام
- [ ] المنتجات والأقسام تظهر
- [ ] أضفت منتجًا للسلة ووصلت إلى صفحة الدفع
- [ ] «إتمام الطلب + إرسال واتساب» يفتح واتساب برسالة كاملة
